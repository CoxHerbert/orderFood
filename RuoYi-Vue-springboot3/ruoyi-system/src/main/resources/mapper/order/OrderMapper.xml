<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.order.mapper.OrderMapper">

    <resultMap type="Order" id="OrderResult">
        <id property="id" column="id" />
        <result property="storeId" column="store_id" />
        <result property="tableId" column="table_id" />
        <result property="userId" column="user_id" />
        <result property="totalAmount" column="total_amount" />
        <result property="payAmount" column="pay_amount" />
        <result property="status" column="status" />
        <result property="remark" column="remark" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <select id="selectOrderById" resultMap="OrderResult">
        select id, store_id, table_id, user_id, total_amount, pay_amount, status, remark, create_time, update_time
        from orders
        where id = #{id}
    </select>

    <insert id="insertOrder" parameterType="Order" useGeneratedKeys="true" keyProperty="id">
        insert into orders(store_id, table_id, user_id, total_amount, pay_amount, status, remark, create_time)
        values(#{storeId}, #{tableId}, #{userId}, #{totalAmount}, #{payAmount}, #{status}, #{remark}, now())
    </insert>

    <update id="updateOrderStatus">
        update orders set status = #{status}, update_time = now()
        where id = #{orderId}
    </update>

    <select id="selectOrdersByStoreAndStatus" resultMap="OrderResult">
        select id, store_id, table_id, user_id, total_amount, pay_amount, status, remark, create_time, update_time
        from orders
        where store_id = #{storeId}
        <if test="status != null and status != ''">
            and status = #{status}
        </if>
        order by create_time desc
    </select>
</mapper>
