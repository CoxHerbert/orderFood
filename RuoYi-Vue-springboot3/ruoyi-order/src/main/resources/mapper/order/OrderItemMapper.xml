<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.order.mapper.OrderItemMapper">

    <resultMap type="OrderItem" id="OrderItemResult">
        <id property="id" column="id" />
        <result property="orderId" column="order_id" />
        <result property="spuId" column="spu_id" />
        <result property="skuId" column="sku_id" />
        <result property="productName" column="product_name" />
        <result property="skuName" column="sku_name" />
        <result property="quantity" column="quantity" />
        <result property="unitPrice" column="unit_price" />
        <result property="totalPrice" column="total_price" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <select id="selectOrderItemsByOrderId" resultMap="OrderItemResult">
        select id, order_id, spu_id, sku_id, product_name, sku_name, quantity, unit_price, total_price, create_time, update_time
        from order_item
        where order_id = #{orderId}
    </select>

    <insert id="batchInsertOrderItems" parameterType="java.util.List">
        insert into order_item(order_id, spu_id, sku_id, product_name, sku_name, quantity, unit_price, total_price, create_time)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.spuId}, #{item.skuId}, #{item.productName}, #{item.skuName}, #{item.quantity}, #{item.unitPrice}, #{item.totalPrice}, now())
        </foreach>
    </insert>
</mapper>
